# wl-app

App is a container element for an WL application. There should only be one `<wl-app>` element per project. An app can have many WL components including menus, headers, content, and footers. The overlay components get appended to the `<wl-app>` when they are presented.

<!-- Auto Generated Below -->


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
